{
  "name": "@ze/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier -w ."
  },
  "dependencies": {
    "@ze/shared": "workspace:*",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}